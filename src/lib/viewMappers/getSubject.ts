/**
 * First `credentialSubject` entry when the property is an array. Moved to
 * `@interop/vc-display`; re-exported here so existing importers are
 * unaffected.
 */
export { getSubject } from '@interop/vc-display'
