export default {
  arrowParens: 'avoid',      // x => x instead of (x) => x for single args
  bracketSameLine: false,    // JSX closing > on its own line
  bracketSpacing: true,      // spaces inside { like: this }
  semi: false,
  singleQuote: true,
  trailingComma: 'none'
}
